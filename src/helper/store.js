import { createStore } from 'vuex';

export default createStore({
    state() {
        return {
            // 상태 정의
        };
    },
    mutations: {
        // 상태를 변경하는 메소드 정의
    },
    actions: {
        // 비동기 로직을 실행하는 메소드 정의
    },
    getters: {
        // 상태에 기반한 계산된 속성 정의
    }
});